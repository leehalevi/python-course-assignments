# print("hi!")
# print("why")

name = input("Enter your name: ")
print(f"Hello, {name}" + "!")

# We can use a replace function
# I think I need Jupyter Notebooks for the classes. Would make it much easier.
# Also fix the GitHub connection
# And figure out how to run code from the terminal

# ctrl + shift + p -> will result in opening explanation about the expression
# so ctrl+/ results in marking anything as a comment!
# ctrl + i opens up co-pilot

# READ ERROR MESSAGES - improtant.
# we can add int + floats. pretty much. sometimes it messes it up.
# You need to make sure that it doesn't affect your code => because when making comparissons, it would not be equal.
# A literal value - type("hi") instead of just "hi"
